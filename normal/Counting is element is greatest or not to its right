def arr(l):
    count=0
    for i in range(len(l)):
        for j in range(i+1,len(l)):
            if(l[i]<l[j]):
                break
        else:
            count+=1
    return count
l=[int(x) for x in input("Enter:").split()]
print(arr(l))
